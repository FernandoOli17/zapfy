---
type: daily
date: 2026-05-29
tags: [daily]
---
# 2026-05-29

## Foco
Ligar a **IA real** (Anthropic + Voyage) localmente e validar ponta a ponta
(Forge real → AgentVersion → conversa de teste → RAG), medir custo por conversa e
rodar eval/anti-alucinação. **Fase 6 — Motor de IA** sai do backlog. Produção
(Fase B) só com OK do usuário.

## Log (mais recente no topo)
- **2026-05-29 (fim de tarde):** Usuário quer **testar manualmente na zapfy.store**
  (Forge + WhatsApp). Diagnóstico → **Fase B**:
  - **#3 Forge real validado** com token (`scripts/forge-generate.ts`): prompt real
    de 4657 chars (não canned). `generateSystemPrompt` extraído pra helper (DRY na engine).
  - **Forge em prod:** só precisa setar `ANTHROPIC_API_KEY` na Vercel + tirar `MOCK_AI`
    + redeploy (instruído o usuário; segredo no painel). VOYAGE opcional pro Forge.
  - **WhatsApp em prod BLOQUEADO → [[BLK-worker-deploy-prod]]:** worker não está
    deployado (sem Railway), fila sem consumidor → agente não responde. Precisa
    deployar worker + número Meta. Forge NÃO depende disso.
  - Tokens na sessão: **~$0.44 (R$2,3)**.
- **2026-05-29 (tarde):** 🤖 **IA REAL ligada (Anthropic) — eval com token rodou.**
  Chave Anthropic válida (smoke Haiku OK). `MOCK_AI=false`. **VOYAGE_API_KEY está
  VAZIA** no `.env` (linha existe, valor len=0) → RAG (#5) bloqueado → [[BLK-voyage-api-key]].
  - **Eval real (#7):** alucinação **0%** ✅ · handoff 100% · tom 100% · tool 80%
    · custo $0.147 (R$0,76) / 6 casos. (rodado via `scripts/eval-ai.ts`.)
  - **Roteamento (#8):** OFF vs ON → qualidade dura idêntica, **custo −20,3%** →
    [[ADR-0004-roteamento-haiku-sonnet]] preenchido. Recomendo validar em escala/staging
    antes de prod. **Decisão do usuário.**
  - **Achado P0 de margem → [[ERR-0002-custo-por-conversa-margem]]:** caso pesado
    custou R$0,45 (tool loop reenviando contexto, cache 0%) vs receita ~R$0,065/conversa
    no STARTER. Mitigações: roteamento, reduzir steps, confirmar cache em prod.
  - **Achado: classifier escala agendamento à toa → [[TASK-0018-classifier-handoff-falso-positivo]]**
    (causa do tool 80%, não foi o agente).
  - Tokens gastos na sessão até aqui: **~$0.41 (R$2,13)** na chave Anthropic.
  - Pendente: #3 Forge real, #4 E2E inbox (precisa worker rodando), #5 RAG (precisa Voyage).
- **2026-05-29 (meio-dia):** 🛠️ **Fase 6 — fundação construída sem token (gate verde,
  53 testes: 19 shared + 34 ai).** Usuário liberou "construir tudo".
  - [[TASK-0012-medidor-custo-token]] medidor de custo (Sonnet/Haiku/Voyage + cache),
    `cachedTokensIn` exposto no runAgent, `costCents` gravado no worker (Message+UsageRecord).
  - [[TASK-0011-detector-alucinacao]] detector de alucinação (P0) — 7 testes.
  - [[TASK-0013-tool-loop-seguro]] tool loop maxSteps+timeout provado com MockLanguageModelV3.
  - [[TASK-0010-eval-harness]] eval harness + 6 conversas douradas + métricas testáveis.
  - [[TASK-0017-roteamento-modelo]] roteamento Haiku→Sonnet (flag `AI_ROUTING`, OFF default,
    wirado no worker) → [[ADR-0004-roteamento-haiku-sonnet]] **proposto** (aguarda eval real + OK).
  - Scripts modo-real prontos (gated MOCK_AI≠true): `eval-ai.ts`, `eval-routing.ts`, `cost-report.ts`.
  - **Aguardando credenciais no .env** pra rodar #4–#8 (eval real, custo, roteamento, E2E, RAG).
- **2026-05-29 (manhã):** Ritual de início. Lidos OPERATING_PROTOCOL, MORNING_BRIEFING,
  Dashboard, Daily anterior, PLAN, AI_ENGINE_PROMPT. **Gate verde confirmado**:
  lint ✅ · typecheck 7/7 ✅ · test 19/19 ✅ (só `@zapfy/shared` tem testes hoje).
  - ⚠️ **Discrepância encontrada:** o prompt de hoje (Fase A2) referencia artefatos
    "da noite" que **não existem no repo nem no vault**: (a) medição de custo de token
    por conversa, (b) eval harness com asserts STUB, (c) ADR de roteamento Haiku→Sonnet.
    A noite real (2026-05-28→29) entregou o **deploy de billing**, não o motor de IA.
    `Fase-6-Motor-IA` segue `status: planned`. Sem eval harness em `packages/ai`
    (nenhum arquivo `*eval*`); só `scripts/smoke-ai.ts` (1 chamada de classifier).
  - Decisão pendente do usuário antes de seguir Fase A2.

## Verdes
- [x] lint  - [x] typecheck (7/7)  - [x] test (53: 19 shared + 34 ai)  - [ ] build (não re-rodado)

## Aguardando você
1. Setar credenciais no `.env` LOCAL (ANTHROPIC_API_KEY, VOYAGE_API_KEY, MOCK_AI=false) e dizer "pronto".
2. Após eval/roteamento reais: decidir [[ADR-0004-roteamento-haiku-sonnet]] (ligar ou não AI_ROUTING).
3. OK pra Fase B (produção) — só depois de ver A+A2.
</content>
</invoke>
